import React, { FC } from "react";
import "./Logo.scss";

const Logo: FC<{}> = () => {
    return (
        <a href="/" className="navbar-logo">
            Logo
        </a>
    );
};

export default Logo;
